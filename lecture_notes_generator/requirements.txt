# lecture_notes_generator/requirements.txt
# FIX H4: Pinned to match backend versions — no pydantic/openai conflicts.
# sentence-transformers and faiss-cpu REMOVED (FIX H1: use Azure embeddings instead).

pdfplumber==0.11.0
python-pptx==0.6.23
openai==1.51.0        # same major as backend
pydantic==2.7.1       # same as backend

# Optional: for image resize in NoteGenerator
# pillow>=10.0.0
